# CXR Report Copilot — Python dependencies
# Pinned for reproducibility; tested on Python 3.10

# ── Deep learning ──────────────────────────────────────────────────────────────
# CPU-only torch (saves ~2GB in the Docker image vs the CUDA wheel)
# For GPU: replace with torch==2.2.0+cu118 and add --index-url https://download.pytorch.org/whl/cu118
torch==2.2.0
torchvision==0.17.0

# ── Image preprocessing ────────────────────────────────────────────────────────
albumentations==1.4.3
Pillow==10.3.0
numpy==1.26.4

# ── API server ─────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9    # required for FastAPI file uploads

grad-cam>=1.5,<2.0
opencv-python<4.10
gradio ==3.50.2

